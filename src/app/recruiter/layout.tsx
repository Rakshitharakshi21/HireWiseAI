import { RecruiterLayout } from "@/components/layouts/recruiter-layout";

export default function RecruiterRootLayout({ children }: { children: React.ReactNode }) {
  return <RecruiterLayout>{children}</RecruiterLayout>;
}
